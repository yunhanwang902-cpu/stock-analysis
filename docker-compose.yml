version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - DEBUG=true
      - CORS_ORIGINS=["*"]
    volumes:
      - ./frontend:/app/frontend:ro
    restart: unless-stopped
